language: python
python:
  - "2.7"
  - "pypy"
env:
  - DJANGO_VERSION=1.5.1
  - DJANGO_SETTINGS_MODULE=boot.settings
install:
  - "pip install -r requirements.txt --use-mirrors"
  - "pip install -q Django==$DJANGO_VERSION --use-mirrors"
script:
  - "nosetests"
